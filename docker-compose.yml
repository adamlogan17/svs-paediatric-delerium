version: "3"

services:
  apis:
    build:
      context: ./APIs
      dockerfile: Dockerfile
    volumes:
      - ./APIs/src:/app/src
    ports:
      - "8000:8000"
  frontend:
    build: 
      context: ./example-frontend
      dockerfile: Dockerfile
    volumes:
      - ./example-frontend/src:/app/src
    ports:
      - "3000:3000"